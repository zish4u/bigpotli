// Categories currently enabled for navigation (nav bar, footer, homepage grid).
// Hijab, Unstitched, Stitched, Lawn Suit, Kurti, and Co-ord Set are temporarily
// hidden from navigation while those collections are built out — the routes
// still exist, they're just not linked anywhere yet. Add slugs back here to
// re-enable them in the menus.
export const VISIBLE_CATEGORY_SLUGS = ["abaya", "pakistani-suit"];
