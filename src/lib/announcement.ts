/**
 * Site-wide announcement strip shown at the very top of every page
 * (src/components/Header.tsx). Set to `null` to hide it entirely, or a
 * string to show it — no code changes needed to swap the message or turn
 * it off/on.
 */
export const SITE_ANNOUNCEMENT: string | null = null;
